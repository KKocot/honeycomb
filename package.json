{
  "name": "hive-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Hive Blockchain UI Components - shadcn/ui style",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:docs": "pnpm --filter docs dev",
    "build:docs": "pnpm --filter docs build"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  }
}
